// 1.Write a JavaScript function to calculate compound interest using the Math.pow() method.
//  Formula: A = P(1 + r/n)^(nt), where P is principal, r is rate, n is compounding frequency, t is time.

// let P = 1000;
// let r = 0.05;
// let n = 4;
// let t = 3;
// let A = P * Math.pow(1 + r / n, n * t);
// console.log("Compound Amount: " + A.toFixed(2));

// 4.Create a function to calculate the circumference of a circle using Math.PI. Allow input of radius and return the
// circumference with 2 decimal places using Math.round().

// let radius = 7;
// let circumference = Math.round(2 * Math.PI * radius * 100) / 100;
// console.log(circumference);

// 7.Create a function that generates random hexadecimal color codes using Math.random()
//  and Math.floor(), returning colors in #RRGGBB format.


// let r = Math.floor(Math.random() * 256).toString(16);
// let g = Math.floor(Math.random() * 256).toString(16);
// let b = Math.floor(Math.random() * 256).toString(16);

// if (r.length === 1) r = "0" + r;
// if (g.length === 1) g = "0" + g;
// if (b.length === 1) b = "0" + b;

// let color = "#" + r + g + b;

// console.log(color);

